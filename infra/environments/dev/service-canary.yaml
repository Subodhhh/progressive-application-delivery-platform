apiVersion: v1
kind: Service
metadata:
  name: releaseforge-app-canary
  namespace: releaseforge
spec:
  selector:
    app: releaseforge-app
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
